import 'package:flutter/material.dart';

class AnimeDetailSection extends StatelessWidget {
  const AnimeDetailSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 500,
      width: 50,
      color: Colors.white,
    );
  }
}
